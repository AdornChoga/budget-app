<li class="border category-item">
  <%= link_to category_transactions_path(category) do %>
    <div>
      <img src="<%= category.icon %>" alt="category_icon">
      <h4><%= category.name %></h4>
    </div>
    <p class="px-3">Total: $<%= category.total_transactions %></p>
  <% end %>
</li>