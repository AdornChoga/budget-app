<%= render 'shared/logged_in_navbar', page: { title: 'Transactions', total: @category.total_transactions  } %>

<ul class="container-fluid-sm top-margin list-unstyled categories">
  <% @financial_transactions.each do |financial_transaction| %>
    <%= render 'shared/transaction', financial_transaction: financial_transaction %>
  <% end %>
</ul>
<%= link_to 'New Transaction', new_category_financial_transaction_path, class: "btn new-trans-btn" %>